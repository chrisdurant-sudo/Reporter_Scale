# Programs workspace candidate

Base: `3a7842908ebf84fb53147d56bb270b0c44423847`
Candidate: `46d0b90a0eb7ba56b5339635d3b7668a00fc9673`
Worktree: `/private/tmp/steno-interview-ip3-programs`
Branch: `codex/interview-ip3-programs`
Preview: `http://127.0.0.1:5191/`

Only `src/features/programs/index.tsx`, `index.test.tsx`, and `programs.css` changed. Clean candidate; no shared, source, data, integration, configuration, dependencies or cross-feature tests changed.

Validation: lint exit0; typecheck exit0; nine owned presentation tests exit0; build exit0 (existing bundle-size advisory); git diff --check exit0. Browser proof at proof-manifest.json:32 checks,14 fixed1440×900/full desktop captures, SHA256 hashes, zero page/console errors. The browser script runs fresh isolated storage and ends with explicit Reset restoring the seed. Previous9f9c343 captures remain in attempt-9f9c343 as history.

## Acceptance evidence

- IP01/IP03/IP06: All, LAX and DFW scope; counted status first-click focus/second clear; combined type/search/sort and clear-all; filter preservation across drawer; chart, table and attention share local scope; exact cohort evidence via shared drawer; exact linked Team navigation.
- IP09/IP11: explicit entered decision rationale, owner, future review; process source selected from real versions; linked work title/owner/domain/due/priority; awaited canonical ports, pending/acknowledgment/error states; unit test rejects failed persistence and preserves input; actual local saves survive reload and reset.
- IP10/XR13 (amended): categorical cohorts replace the connected trend; All6/20→11/20 and LAX3/10→6/10; mature/observing denominators, horizon, outcome units; compatibility-aware target labels; whole-program target remains benchmark-only for LAX. DFW1/6 qualification and $3,600 recorded spend remain inspectable; ongoing referral cost remains unavailable with source reason. No derived business metrics or stored chart totals.
- IP11: problem, hypothesis, eligibility, recorded process versions/trigger/steps/role/SLA/evidence/exception, absent historical fields, workaround table, participant onboarding evidence and linked canonical Team work. Administrative stage stays distinct from completed observation. No causal or automatic rollout claim.
- IP12/XR17/XR40: root-ready typed commands, source values and dates, notes/next step whitespace and explicit clearing; all locked type categories; canonical source labels. Root weekly review unchanged.

Directly inspected historical reference manifest and fixed Programs reference, amended interactive reference plus default/checklist/decision screenshots; compared candidate desktop/full compositions. Lead review requested, pending final disposition. Shared CategoryComparison baseline differs from reference in bar color/scale and typography; flagged to Lead, never locally overridden. Candidate table begins at approximately909px vs reference833px; Lead reviewing density.

## Root integration follow-up

Four old actions remain optional/deprecated and unused purely to compile unchanged V2App. Root removes its factories/action properties after integration, then requests owned removal of these declarations. No legacy callback is called.

Root integration test labels:

- Open `Readiness checklist pilot` (attention and table both expose it), then `Record decision`. Fields: `Decision` (continue/change/stop/expand), `Accountable owner`, `Next review date`, `Decision rationale`; submit `Save decision`.
- `Save process draft`: `Source process version` (canonical ID values; labels Version N · status), `Accountable owner`, `Next review date`; submit `Save draft`.
- `Create linked work`: `Work title`, `Owner` (empty=Unassigned), `Domain`, `Due date`, `Priority`; submit `Save linked work`.
- `Edit notes & next step`: accessible fields `Notes for <program>` and `Next step for <program>`; submit `Save notes and next step`.
- `Back to program`, dialog close button, and Escape retain local filters; saves retain explicit inputs on failure. All form dates use17:00 UTC and validate against supplied current demo command time, never historical evidence time.

Coordinator lane/governance integration and independent IP5 Quality/IP6 Reviewer remain required. No self-merge or deployment.
