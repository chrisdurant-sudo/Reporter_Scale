# Unattended coordinator handoff — September 19, 2026

The user explicitly requested a new task with a never-interrupt config for continuing the already-authorized interview implementation while away. Keep the active repository at `/private/tmp/steno-p4-coordinator`, branch `codex/reporter-growth-p4-coordinator`; do not use the older Desktop/STENO source. The user only needs desktop (1440×900 pointer/keyboard), not tablets or phones.

## Approved permission setup

The requested noninteractive approval setting is `approval_policy = "never"`. To avoid the already-observed sandbox failures for protected Git writes, external symlinked build caches and Chromium, the approved companion is `sandbox_mode = "danger-full-access"`. Full Access removes the shell filesystem/network sandbox and is broader than suppressing prompts; the user explicitly approved “Approve Full Access for this project task” on September 19. Both settings are now applied in this project’s `.codex/config.toml`. Keep `service_tier = "default"` and all existing Astra role assignments. Do not alter global user configuration. A replacement task must verify the **actual loaded permissions**, not infer them from this file. App or managed overrides may prevail. Even never mode cannot guarantee no outage, usage limit, denied app/tool consent, or substantive blocking decision.

## Exact current state

- Current coordinator source/history HEAD before this handoff: `61715bf` (resolve full SHA). `AGENTS.md`, `ASTRA_COORDINATOR_HANDOFF.md`, `P4_EXECUTION_STATE.json` and `lanes.v2.json` are authoritative.
- Phase `IP2_visual_proof`; fanout is false. Data/Capacity/Recruiting/Network/Team/Programs and command composition are integrated; baseline `dfa4b9b921991abd6a40127146520c421204c8cb` passed 307 unit/integration, nine acceptance and eight browser tests, plus governance/lint/types/build.
- The Experience Lead has completed proof and stopped all edits: `/root/ip2_experience`, worktree `/private/tmp/steno-interview-ip2-experience`, branch `codex/interview-ip2-experience`, audit base `f7a6fc734513ba8621a7dbf3b1845f818d420658`, clean source candidate `be5d7a1d3a634f4a29243e09993661cef7264c1c`. Allowed ten changed files are under UI, shell, styles, Markets and Recruiting. Source edits have stopped. Its final handoff is copied to `docs/reporter-growth/v2/IP2_EXPERIENCE_SOURCE_HANDOFF.md`. Final evidence is in `/private/tmp/steno-ip2-source-proof`; use the final handoff/manifest there, not earlier dirty captures. Its Vite preview is port5186, session62652 in the old worker; restart if needed.
- Root owns newer attention test correction `d4419f0`, not in the Lead base/candidate: source-backed attention-row Why this selector and exact displayed window/as-of date. Root typecheck/lint pass; runtime pending integration. Do not change worker candidate to include this assertion; merge its exact SHA into coordinator to retain ancestry.
- Lead lane audit, owned tests, types, lint/build passed. Its full unit run has only the known root attention-selector failure. Existing acceptance7/9 and browser4/8 fail superseded KPI/chart/status semantics and unscoped Why this selectors. Do not preserve incorrect product semantics or waive tests to pass these.
- User explicitly approved “Approve these bounded test-alignment passes.” `IP2_TEST_ALIGNMENT_REQUEST.md` and ledger `interview_improvement_amendment.test_alignment` now authorize serial Quality-owned alignment of the two named tests through IP4. Gates and eighteen governance tests enforce exact-candidate diagnostic/scope, stopped workers, verified Quality runtime, no waivers, and retained independent Quality/Reviewer. No further sequencing approval is needed.

## Immediate next steps

1. Obtain the stopped Lead handoff, independently compare exact clean desktop captures to `docs/reporter-growth/v2/design-lock/reference-manifest.md` and the amended package `docs/reporter-growth/v2/design-lock/interview-2026-09-19/reference-manifest.json`. Reference freeze is `d4b4ff98a3ee9e9fd1bffe829a6b747ab34670d1`. Historical and amended reference hashes must remain unchanged; desktop-only scope supersedes supplementary tablet captures.
2. Run lane audit and governance, merge the exact Lead candidate sequentially, run complete verification, preserve initial failures as diagnostics without overwriting the passed serial-preparation baseline. Prepare the authorized first Quality test-alignment pass with its exact integrated source, checksum-valid diagnostic and scope; stop other workers, then dispatch Quality with zero inheritance and verified Astra/xhigh runtime. It owns only `tests/acceptance/reporter-growth.integration.test.tsx` and `tests/e2e/reporter-growth.browser.spec.ts`. Preserve assertions/coverage, no skips or timeout inflation. Integrate audited candidate and run full suite. Only then accept visual proof/open IP3.
3. Probe the three named Experience specialist roles serially, verify actual Astra/high runtime metadata and zero inherited turns. Existing Lead probe verified. Use `fork_turns="none"` on every worker/probe; no model overrides or child spawning. Named role configuration remains Standard/default; actual resolved tier is unobservable, disclose limitation rather than claiming it verified.
4. Lead as sole shared writer/steward plus three specialists can run together after proof: Reporters owns `src/features/reporters/`, Team `src/features/team/`, Programs `src/features/programs/`. Each gets one exact brief, exact base/contract commit, sample addendum, both reference paths, desktop scope, relevant IP/XR criteria, isolated worktree/branch and exact-commit handoff. Lead must review candidate/reference screenshots before each integration.
5. Read IP1_NETWORK_HANDOFF.md, IP1_TEAM_HANDOFF.md, IP1_PROGRAMS_HANDOFF.md and V2App top-level prepared ports. Specialists must declare/use explicit entered forms and canonical async commands, catch failures and retain inputs. Remove obsolete one-click Network and Programs factories from root composition after forms integrate. Root owns integration tests. No raw snapshot, shadow business calculations or sibling/integration imports in feature screens.
6. Full integrated verification, independent IP5 Quality (IP01–IP12 plus applicable XR/SD/original acceptance), owner repairs/rechecks, IP6 read-only Reviewer on exact Quality-passed commit, then five-minute walkthrough rehearsal. No deployment or main checkout merge.

## Contracts that must stay intact

- Completed 50-person expansion is not repeated:113 baseline people,114 scenario;25 linked tasks. Preserve SD01–SD07.
- Readiness, availability, acceptance, completed jobs are different. Requested=confirmed+unresolved; possible/unverified/unknown are exclusive unresolved subsets. Current schedule and original LAX ten-slot story remain distinct.
- Funnel January M08 All17/51,LAX9/22 within14days; February LAX0 mature+2 observing. Status first-click focuses, second clears; owner/waiting stable IDs; operational filters separate from entry/source cohorts. Exact external drill temporarily supersedes retained local filters; clear restores.
- Network explicit availability markets/modes/time/expiry and follow-up market/nullable owner/due. Repeated follow-up opens the same canonical task. Prepared skills, preferences, credentials and recent work are separate; inactivity is not churn.
- Team three workflow columns; blocked separate. Edit/status/owner/due/priority/notes persistence/reset and evidence-required completion. Historic completion actor immutable. Whole-member targets cannot silently be compared against market subsets. M11 counts inspected tasks, separate pass ratio. Current actor/time/IDs come from coordinator context.
- Checklist All6/20→11/20, LAX3/10→6/10,SFO3/10→5/10,14-day horizon,50% whole-program target. Categorical descriptive comparisons, no invented trend/causality. Administrative reviewing vs completed cohort observation. DFW qualification1/6,40% target,$3600direct spend. Referral2 observing, unavailable cost. Decisions need entered owner/rationale/future review; process drafts copy real canonical version; no automatic rollout.
- Weekly review is market-wide at current time, independent of local record/demand filters, with explicit actual source windows and comparable-period limitations. Due dates are not review dates; March16 checklist review remains actual.

## Environment and verification

Node dependencies symlink to `/Users/pc/Desktop/STENO/node_modules`; no new packages. Chromium executable `/private/tmp/steno-playwright-headless-shell/chrome-headless-shell-mac-arm64/chrome-headless-shell`.

Full gate: `env PLAYWRIGHT_CHROMIUM_EXECUTABLE_PATH=/private/tmp/steno-playwright-headless-shell/chrome-headless-shell-mac-arm64/chrome-headless-shell npm run verify:p4` (redirect to a unique `/private/tmp` log). Use explicit correct cwd for every command. Existing package unit script uses two workers. Governance runs only in recorded coordinator checkout; lane checks inspect exact base/candidate. Root helper scripts `/private/tmp/steno-ip-record-probe.py` and `/private/tmp/steno-ip-record-worker.py` record session metadata; inspect before use. Do not use verification recorder to overwrite the passed preparation baseline with a later expected test-alignment failure.

Preserve user work in `/Users/pc/Desktop/STENO` (old source, preexisting config edits, deleted reporter files, untracked JD.txt). No blanket staging, destructive cleanup, force push, dependency change, deployment or external messages. The new task takes over after the original coordinator and Lead stop editing.

## Transfer checkpoint

Lead is stopped at P4_VISUAL_PROOF_READY, with16 exact desktop artifacts and19 passing interaction checks, no console/page errors. Coordinator has not yet accepted proof or integrated the candidate. All worker test/proof processes are complete; the preview alone remains live. This original coordinator will stop production edits when the replacement task is created. The replacement owns subsequent work. Runtime permission verification should occur before any worker dispatch: expected never + danger-full-access. If app overrides persist, report the exact loaded mode and request the user select Full access in the new task rather than claiming TOML text changed the running session.
