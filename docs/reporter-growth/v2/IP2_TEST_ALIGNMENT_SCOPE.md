# IP2 bounded test-alignment scope

This is the approved serial test-owner exception from `IP2_TEST_ALIGNMENT_REQUEST.md`, not the independent IP5 Quality gate. Dispatch requires the stopped, clean Lead repair, exact integrated-candidate diagnostics and a prepared ledger entry with this file's checksum.

Allowed files only:

- `tests/acceptance/reporter-growth.integration.test.tsx`
- `tests/e2e/reporter-growth.browser.spec.ts`

| Historical assertion | Authorized replacement | Coverage retained |
|---|---|---|
| Overview's Markets / Available reporters / Open jobs / More needed KPI labels | The four explicit schedule cells: requested slots, confirmed slots, unresolved slots, readiness versus goal or an explicit unsaved goal. Check their source-backed values and requested = confirmed + unresolved. | Four-cell hierarchy, slot/person distinction and unchanged navigation. |
| Static chart `img` role and older accessible title | Exact named interactive `group` for the amended chart. Exercise its keyboard observation and check date, value, unit and observed/projected status where applicable. | Real data chart, visible observations, range/status selection, projection state. |
| Global Overview row count | Scope rows to the Market comparison table. | Selected LAX yields the correct single market row; unrelated new review/schedule structure does not invalidate the table count. |
| First Applicant status click excludes Applicants | First click focuses Applicant; validate active state and matching displayed rows/count. Second click clears and restores the prior inventory. | Status filtering and clearing, notes, owner/waiting semantics and source safety. |
| A single unscoped `Why this?` trigger | Select the intended attention item and its evidence action using a meaningful container/record scope. | Exact contextual evidence, modal keyboard access, Escape/focus return and existing viewport behavior. |

Read the exact diagnostics and candidate before editing. The initial integrated candidate reproduces two acceptance failures and four browser failures (the latter are the same two cases in desktop and mobile). Assertions later in these failing cases have also been inspected against the amended source and are listed above so the pass can correct the complete known conflict without iterative scope creep.

Retain every unaffected workspace, synthetic disclosure, market persistence, no-overflow, note editing/persistence, SLA market isolation, lifecycle noninterference and evidence check. Keep both existing browser projects; current desktop-only visual-proof scope does not authorize silently deleting existing browser coverage. Do not skip cases, add waivers, inflate timeouts, use broad matches that evade the intended assertion, or edit production/configuration/dependencies. Any unrelated failure returns to its owner.

The worker stops at an exact test-only commit with changed-path audit and actual acceptance/browser/lint/type results. Coordinator independently inspects the diff, integrates sequentially and runs the full suite. The final independent IP5 Quality and IP6 Reviewer gates remain required.
