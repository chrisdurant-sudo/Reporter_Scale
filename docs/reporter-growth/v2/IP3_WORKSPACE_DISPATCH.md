# Interview workspace wave — September 19

The coordinator accepted revised Overview/Funnel proof from clean source
`055fcd18a39a6238d6beb7fc52bdd03a315ea1c3`. The integrated source baseline
`b5cbe33e66a52f466456467a240d2f704208fb10` passes the full suite:315 unit/integration,
9 acceptance,8 browser, governance, lint, types and build. The execution ledger
contains exact evidence and actual Lead/specialist Astra/high runtime probes.
Each dispatch supplies the exact descendant documentation/base commit and separate worktree.

## Frozen source and references

- Serial contract/data/logic freeze: `dfa4b9b921991abd6a40127146520c421204c8cb`.
- Exactly one role brief: `tasks/experience-reporters.md`, `tasks/experience-team.md`,
  or `tasks/experience-programs.md`; Lead retains its existing brief.
- Read `AGENTS.md`, `FOUNDATION_REVIEW.md`, `P1_SOURCE_CONTRACT.md`, `LANES.md`,
  `lanes.v2.json`, `PRODUCT_BRIEF.md`, `DATA_CONTRACT.md`, `METRICS_AND_EVIDENCE.md`,
  `SCREEN_CONTRACTS.md`, `P4_READINESS.md`, `P4_DESIGN_LOCK.md`, `P4_ACCEPTANCE.md`,
  `P4_INTERVIEW_IMPROVEMENT_PLAN.md`, `P4_INTERVIEW_CONTRACT_DELTAS.md`,
  `P4_INTERVIEW_DESKTOP_SCOPE.md`, and `P4_SYNTHETIC_SAMPLE_EXPANSION.md`.
- Required historical reference: `docs/reporter-growth/v2/design-lock/reference-manifest.md`.
- Versioned amendment: `docs/reporter-growth/v2/design-lock/interview-2026-09-19/reference-manifest.json`
  (SHA256 `f5a1443b3705b8bb5a5bdd50ef89d9e5e0ca44520172de17a01780ace79b268f`).
  Directly inspect the interactive reference and fixed screenshots, then compare candidate captures.
  The amendment supersedes changed states; preserve the historical package.
- Accepted shared proof: `design-lock/candidate/055fcd18a39a6238d6beb7fc52bdd03a315ea1c3/proof-manifest.json`.

## Ownership and shared system

Each specialist owns presentation and colocated tests only in its assigned feature directory.
The Lead alone owns `src/ui/`, `src/shell/`, `src/styles/`, Overview and Funnel.
No specialist edits shared styles, contracts, integration, data, logic, other features, instructions,
dependencies or cross-feature acceptance/browser tests. Everyone is working alongside other agents;
preserve their changes. No child agents, self-merge, external writes or deployment.

Use `Panel`, `Drawer`, `TimeSeriesChart` and `CategoryComparison` from `src/ui/interview.tsx`,
shared date/number formatting, and the frozen `ip2-*` classes. Do not copy primitives or create
local tokens. Send concrete component questions to `/root/experience_probe_takeover`; the Lead
must broadcast any approved shared change. For evidence/navigation from a local drawer, close it
before invoking the root callback (the accepted pattern uses requestAnimationFrame), preserving focus.

Only display prepared source values. Typed asynchronous commands must be awaited; keep input on
failure and show actionable errors. Actor and operation time come from `commandContext`, not
historical evidence. No shadow records or stored chart totals. Existing sample is complete:
113 baseline identities,114 with the scenario; do not expand it again.

## Domain guidance

- Reporters: read `IP1_NETWORK_HANDOFF.md`. Separate skills, preferences, credential evidence and
  explicit availability. Service-market browsing differs from actual job-market evidence. Use
  `onRecordAvailability`, `onRequestFollowUp`, supplied navigation targets and command context.
  Availability requires actual selected markets/modes/window/expiry; follow-up requires explicit
  market, nullable owner and due date. Repeating a follow-up opens the existing canonical Team item.
  Retain exact person evidence and recruiting-checklist actions. Primary IP07/IP09 plus IP01/IP03/IP06/IP12.
- Team: read `IP1_TEAM_HANDOFF.md`. Three columns with blocked as a condition, prepared member/domain/
  program/blocked filters, canonical work edit/transition, explicit assignment/due/priority/notes,
  history and evidence-backed completion. Preserve historical completion actor. Goals must show
  role/window compatibility; M11 count and quality ratio have distinct units. Use supplied callbacks
  and actor context for goal, quality, coaching and practice forms. Primary IP05/IP06/IP09 plus IP01/IP03/IP12.
- Programs: read `IP1_PROGRAMS_HANDOFF.md`. Use categorical cohort comparisons; legacy resultOverTime
  is not a time series. Respect each metric (M07 qualification, M12 first jobs, M09 currency minor units),
  numerator/denominator/window and target compatibility. Checklist before/after is All6/20→11/20,
  LAX3/10→6/10, SFO3/10→5/10; whole-cohort50% target is not a subset-compatible target-met claim.
  Explicit rationale/owner/next-review decision form uses `onSaveDecision`. Draft form selects a real
  canonical process version and uses `onSaveDraft`; linked Team work uses `onCreateLinkedWork`.
  Preserve checklist steps/owner/SLA/evidence/exceptions/workarounds and canonical notes/next steps.
  Primary IP10/IP11 plus IP01/IP03/IP06/IP09/IP12. No automatic rollout or causal claims.

## Evidence and handoff

Required proof is1440×900 desktop with pointer/keyboard, including default, filtered and working-form
states. Preserve basic responsiveness and existing browser assertions; no separate phone/tablet
optimization or capture pass. Use a unique localhost port and the existing dependencies; Chromium:
`/private/tmp/steno-playwright-headless-shell/chrome-headless-shell-mac-arm64/chrome-headless-shell`.
Eligible escalations use the active automatic reviewer. Do not weaken sandbox settings.

Run lint/types/build and meaningful owned tests, then commit only the assigned path and stop edits.
Capture the exact clean commit, record screenshot SHA256 values and interaction results, and send the
Lead candidate and reference screenshots for review. Resolve findings only in your own path.
Return `P4_WORKSPACE_REVIEW_READY`, exact base/candidate, tests/exit codes, IP/XR coverage, reference
comparison, proof/Lead review paths and remaining gaps. The coordinator runs the lane audit and
governance before each sequential integration. Independent IP5 Quality and IP6 Reviewer remain required.
