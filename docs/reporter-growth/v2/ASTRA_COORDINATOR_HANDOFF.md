# Astra coordinator handoff — September 18, 2026

> IP1 Network update: Network candidate `67fbed109a7306204c692dcb1b509f6560591621` is integrated. Exact coordinator candidate `4e2a715041643c2c9ccc428086258e8f77fb894b` passes 241 unit/integration, nine acceptance and eight browser tests. Team and Programs remain next, serially; command composition and presentation remain pending. Read `IP1_NETWORK_HANDOFF.md` and the active ledger.

> Continuation update: the user has now explicitly authorized the improvement implementation in task `01a0b79d-03f1-7f60-9d63-681a9f9baef3`. Start from `P4_INTERVIEW_CONTRACT_DELTAS.md` and the active ledger. Fresh coordinator task `01a0b7df-3f9e-7c72-a35a-1a6ed497e596` is rooted at `/private/tmp/steno-p4-coordinator` and has verified the loaded Astra catalog plus Data and Quality zero-inheritance probes. Chromium launch is recovered. The user-approved Quality-only IP0 browser-suite repair is integrated at `30a1aca9fbee1b1c4e81a5e6841fbd8b81a81b1a`; the full suite passes. The current phase is IP1 serial Data/logic implementation. Read the live ledger and `IP0_BROWSER_REPAIR_REQUEST.md` for exact evidence. Do not request another implementation start or repeat the 50-person expansion.

> IP1 update: Data, browser persistence, Capacity IC01 and Recruiting IC02 are integrated. The follow-up Data history-validation defect is repaired; malformed previous values now fail load/save without replacing bytes. Latest full baseline `702e95a8364232d03c913460d606970aaed1e7e0` passes 207 unit/integration, nine acceptance and eight browser tests. The completed 50-person expansion and 25-task sample are unchanged. Next serial roles: Network, Team, Programs. Recruiting API/cohort evidence is in `IP1_RECRUITING_HANDOFF.md`. New presentation and independent Quality/Reviewer gates remain pending.

## Start in the correct checkout

Use `/private/tmp/steno-p4-coordinator`, the checkout behind the reviewed local preview at
`http://127.0.0.1:5174/`. Verify its current `git status`, branch and HEAD before proceeding.
The source/doc baseline before this planning amendment was
`f2b3357a2b20cd5ce7effde7d29827f09fab9e6c`. Planning changes are layered on that candidate.
The main `/Users/pc/Desktop/STENO` checkout remains at the older `8b85d4aafa2317be848e01caf47d8b6f92efa674`.
Do not overwrite the newer source with that older checkout or assume two chats are separate worktrees.
The active path is in temporary storage; verify it exists before starting and preserve it before any
cleanup. No merge or deployment has been performed for this planning update.

The main checkout has pre-existing `.codex/config.toml` edits, deleted reporter feature files and an
untracked `JD.txt`; preserve them. This session could not modify its protected `.codex` directory.
The requested model configuration is therefore installed in the active candidate checkout only.
The root contains copies of this handoff, the improvement plan and product review for discoverability.
A new task must use the active checkout's project configuration; opening an Astra task in the older
main checkout alone will not load the new worker assignments.

## What the user authorized

Detailed critique first, then incorporate the proposal into the plan and update model assignments.
The new interview improvement implementation has not been authorized or started by that request.
The user intends to select Astra/xhigh as coordinator. Keep All / LAX / SFO / DFW / ORD / ATL;
LAX is the primary narrative, while All and every other market remain functional and meaningful.
No production source, dependencies, deployment or external messages changed in this planning pass.

## Read first

1. `P4_INTERVIEW_IMPROVEMENT_PLAN.md` — actionable scope, JD mapping, IP01–IP12 and delivery gates.
2. `INTERVIEW_PRODUCT_REVIEW_2026-09-18.md` — observed behavior and reasoning behind the changes.
3. `P4_READINESS.md`, `P4_DESIGN_LOCK.md`, `P4_SYNTHETIC_SAMPLE_EXPANSION.md`,
   `design-lock/reference-manifest.md`, `P4_EXPERIENCE_REDESIGN.md`, `P4_ACCEPTANCE.md`.
4. `P4_EXECUTION_STATE.json`, `lanes.v2.json`, `P4_LANE_HANDOFF_CONTRACT.md`, foundation and source
   contracts required by AGENTS.md. The September 18 amendment supersedes only specified old
   presentation requirements; canonical data meanings and safety boundaries remain in force.
5. Job description: `/Users/pc/Desktop/STENO/JD.txt` (user-owned, not staged by this planning pass).

## Current evidence and remaining work

The completed 50-person expansion and SD01–SD07 are historical passed gates. Do not rerun an additive
50-person seed expansion. A bounded linked-task/history amendment may be needed for the Team board.
Historical Overview/Funnel and integrated workspace proof exist, but do not cover IP01–IP12.
The last source repair was `16479473d6b22ed64a18d4b1b5b9748f1c84374b`; independent Quality rerun and
Reviewer are still unclosed in the ledger. Do not report the prior P4 candidate or this new scope complete.
The product audit was desktop-focused and did not constitute a fresh independent full test run.

After an explicit implementation start for this amendment: record it, verify routing, settle bounded
contracts/data first, accept new Overview/Funnel visual and behavioral proof, then use the existing
Lead-plus-three-specialists wave. Quality tests the assembled exact commit; owners repair; Reviewer
starts only after Quality passes without waivers. Preserve the historical reference package and hashes;
version new amended references before specialist implementation. A larger model does not replace
checking the actual rendered charts, every filter and the manager workflows against source records.

## Configured model assignments

- Coordinator: select `gpt-6-astra` / `xhigh` interactively; no forced root-model override.
- Experience Lead, all three Experience specialists, Data and every domain support role:
  `gpt-6-astra` / `high`.
- Quality and Reviewer: `gpt-6-astra` / `xhigh`.
- Every role and project config: `service_tier = "default"` (Standard).

Registry, role TOMLs, active model tables and readiness validator agree. Current running tasks and
already loaded role definitions are not changed retroactively. Start fresh and verify actual runtime
model/reasoning metadata with zero-inheritance probes before any worker. Do not silently substitute a
model. Resolved child tier is not exposed; configured default remains the observable tier evidence.

## Suggested opening message for the new task

“Use `/private/tmp/steno-p4-coordinator` as the active checkout. Read
`docs/reporter-growth/v2/ASTRA_COORDINATOR_HANDOFF.md` and the linked interview improvement plan.
Verify the current state and Astra/Standard configuration, then report readiness and remaining gates.
Preserve All-market filtering with LAX as the primary through-line. Do not begin implementation until
I explicitly authorize the improvement pass.”

Use `npm run verify:p4:governance` for this planning/configuration check. Before implementation phase
transitions use the full required `npm run verify:p4` and exact-commit visual/interaction proof.
No credit reset was requested or redeemed during this work.

## Planning-pass verification

September 18: readiness 193/193, phase gate 292/292, TOML parsing and all 12 registry/role model-reasoning-tier comparisons passed; `git diff --check` passed. These are planning/configuration checks, not a fresh application or browser test pass.
