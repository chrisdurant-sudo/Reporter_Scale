# Prepared request: Quality-owned interview assertion alignment

Status: **draft sequencing request; not authorized for dispatch**. Attach the exact Lead candidate and failing full-run diagnostic before requesting the narrow exception. The approved IP0 repair is closed and does not authorize this work.

The current interview amendment explicitly changes Overview KPIs and normal status-filter behavior. Two independent Quality-owned files still assert their superseded presentation. AGENTS.md requires a passing full suite before the next phase but schedules Quality after integrated presentation. This request concerns test ownership/sequencing only; it cannot waive an assertion or start the later independent Quality gate.

## Concrete conflicts verified in the current test source

| File and case | Current assertion | Required amended assertion |
|---|---|---|
| `tests/acceptance/reporter-growth.integration.test.tsx`, Overview shell case | Four labels match Markets / Available reporters / Open jobs / More needed | Four source-backed capacity cells for the explicit schedule: requested, confirmed, unresolved and readiness versus saved goal (or explicit no-goal state). Reconcile slots and retain source meaning. |
| Same file, Funnel controls case | First click on counted Applicant button has `aria-pressed=false` | First click focuses Applicants with matching records/count; second click clears to All. Owner and Waiting-on remain independent stable keys. |
| `tests/e2e/reporter-growth.browser.spec.ts`, Funnel controls case | Same first-click exclusion assertion | Same focus/clear behavior tested in both desktop and 390px projects, retaining notes, range/status selection, market-specific SLA and lifecycle noninterference. |
| Existing Overview and chart selectors in these same cases | Global row count and historical chart names assume the older composition | Scope row assertions to the intended market grid and select the correctly labeled source-backed chart/control. Retain the same underlying market and navigation assertions. |

## Proposed bounded exception

After the exact new candidate reproduces these conflicts, allow one serial named Quality worker with the verified Astra/xhigh zero-inheritance routing, an isolated worktree and exact base. It may edit only the two files named above to align the affected Overview/Funnel assertions and selectors with the authorized amendment. It may not edit production, source data, metric contracts, references, dependencies, unrelated tests or governance. Any unrelated failure is returned to its owner. No skip, waiver, broad text match that avoids the intended assertion, timeout inflation or weakening of source/interaction coverage.

The worker must inspect the frozen amended reference, exact candidate, prepared records and failure diagnostics; retain synthetic boundaries, all market navigation, evidence/focus, no-overflow, note persistence and SLA/source safety checks. It stops at an exact candidate and passes the Quality lane boundary audit. Coordinator integrates sequentially and reruns the complete suite. The full independent IP5 Quality review of IP01–IP12 plus applicable XR/SD/original criteria still runs after all specialist integration, followed by the exact-commit Reviewer.

No user decision has been requested or recorded for this draft. Candidate SHA, diagnostic evidence and final changed-test scope remain required before approval is sought.
