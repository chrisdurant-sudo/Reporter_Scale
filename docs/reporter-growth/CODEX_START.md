# Paste into the main Codex session

> **Version 1 legacy build entry point.** Do not run this prompt during the v2
> documentation-only foundation phase. The active v2 planning pointer is
> `docs/reporter-growth/v2/START_HERE.md`; implementation requires separate authorization.

You are the coordinator. Build the Reporter Growth MVP from the planning packet in docs/reporter-growth/.
The packet contains instructions only; you are responsible for creating and testing the application.

Read AGENTS.md, docs/reporter-growth/BUILD_PLAN.md, LANES.md, AGENT_ROUTING.md, lanes.json, CONTRACTS.md, PRODUCT.md,
DESIGN.md and ACCEPTANCE.md. Keep React + TypeScript + Vite, three screens, five-market filtering,
plain language, minimal clutter, and synthetic data. Do not reopen settled scope or construct an agent harness.

First complete the short serial setup: inspect the actual repo/instructions and preserve existing work;
choose compatible dependencies once; create the minimal compiling foundation, exact shared interfaces,
public exports, typed temporary boundary stubs, test commands and real lockfile. Commit a verified base.
Freeze the contracts, write scopes and acceptance anchors before launching parallel writers.

All lane and reviewer spawns must use the named agent role with
`fork_turns="none"` or a bounded positive value. Never use
`fork_turns="all"`.
Before full fan-out, perform the model-routing probe in AGENT_ROUTING.md: spawn one tiny `data`-role probe and verify, where the runtime exposes it, that the child is actually using the configured Luna/medium policy. If it inherits the coordinator configuration, the requested model is unavailable, or routing cannot be verified, do not blindly launch the full swarm. Report and correct the routing path first. Do not let workers independently raise their model/reasoning tier.


Then use the maximum useful number of ready independent lanes permitted by the plan AND this runtime.
There are seven lanes and matching Codex roles: experience, data, logic, markets, reporters, improvements, quality.
The project cap is seven concurrent lane workers plus one optional read-only reviewer, excluding you.
Use fewer if actual support, permissions, resource limits or ready work requires it. Workers cannot spawn children.

Actually delegate with the supported subagent tools; do not merely print assignments.
Give each worker one separate verified Git worktree/branch, absolute path, base commit, lane task,
allowed paths, interface revision, acceptance IDs and handoff requirements. A spawned thread is not proof of isolation.
If permitted separate working areas cannot be established, run serially and explain the limitation.
Do not change account settings or weaken sandbox/approval protections to increase concurrency.

Keep shared contracts, root configuration, dependencies, integration and merges under your sole ownership.
Screen workers use prepared props and callbacks, not sibling features, storage or canonical logic.
Route contract requests centrally, pause affected lanes, and update the shared baseline before resuming them.
Limit simultaneous heavy builds/browser suites to two. Keep coordination short and record-backed.

Workers must stop edits and return REVIEW_READY at an exact committed candidate with actual check evidence and gaps.
Review the scope and implementation independently, integrate approved commits sequentially, and route domain defects
back to their owners. Quality writes cross-feature tests but cannot change production code or approve its own work.

Run the full acceptance, type/build, unit and browser checks on one assembled commit; inspect mobile/desktop and keyboard use;
obtain a fresh read-only review of that same commit. Remove all temporary stubs. Claim MVP_DONE only with passing evidence.
If blocked, report what works and the exact blocker. Do not invent agents, installs or passing tests.

Start setup now and continue through the local MVP. Ask only for genuinely missing permissions or a material product
choice unresolved by the packet, not routine implementation choices. No push, deployment, real messages or real service writes.
